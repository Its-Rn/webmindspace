[build]
  command = "npm --prefix client install --include=dev && npm --prefix client run build"
  publish = "client/dist"

[[redirects]]
  from = "/api/*"
  to = "/api"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
